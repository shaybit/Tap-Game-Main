
---
name: Build and Push Python Image to Google Cloud Platform
on:
  push:
    paths:
        - server/*
    branches: [ master ]
jobs:
  build-push-gcr:
    name: Build and Push to GCP
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: tap-game-backend
      PROJECT_ID: tap-game-372914
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    
    - id: 'auth'
      name: 'Authenticate to Google Cloud'
      uses: 'google-github-actions/auth@v1'
      with:
        credentials_json: '${{ secrets.SERVICE_ACCOUNT_KEY }}'

    - name: Build Docker Image
      run: docker build  -t $IMAGE_NAME:latest .
      working-directory: server
 

    - name: Configure Docker Client
      run: |-
        gcloud auth configure-docker --quiet
        gcloud auth configure-docker us-west2-docker.pkg.dev --quiet

    - name: Push Docker Image to Container Registry (GCR)
      env:
        GIT_TAG: ${{ steps.increment-git-tag.outputs.git-tag }}
      run: |-
        docker tag $IMAGE_NAME:latest gcr.io/$PROJECT_ID/$IMAGE_NAME:latest
        docker push gcr.io/$PROJECT_ID/$IMAGE_NAME:latest
      working-directory: server

    - name: 'deploy to gcr'
      run: gcloud beta run deploy demo-app --image gcr.io/$PROJECT_ID/$IMAGE_NAME:latest --region us-central1 --platform managed --allow-unauthenticated --quiet
    
    - id: 'deploy'
      uses: 'google-github-actions/deploy-cloudrun@v1'
      with:
        service: 'tap-game-backend'
        image: gcr.io/$PROJECT_ID/tap-game-backend/$IMAGE_NAME:latest
    
   
      

        


